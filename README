Install environment: conda env create -f env.yml (or install packages named in env.yml from PyPI)

Activate the environment: activate emg-plot

Run the plot demo: python main.py

Run the plot (full): python main.py --host raspberry_b27
